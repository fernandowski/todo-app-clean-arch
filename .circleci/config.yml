version: 2.1
jobs:
  build-and-test:
    docker:
      - image: cimg/base:2022.09
    steps:
      - checkout
      - run:
          name: Execute Units
          command: |
            docker build . --target=unit-test
workflows:
  build-test-workflow:
    jobs:
      - build-and-test
